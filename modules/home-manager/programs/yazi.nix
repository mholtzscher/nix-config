{
  ...
}:
{
  programs = {
    yazi = {
      enable = true;

      # Theme is managed by catppuccin

      settings = {
        mgr = {
          show_hidden = true;
          # sort_by = "mtime";
          # sort_dir_first = true;
          # sort_reverse = true;
        };
      };
    };
  };
}
