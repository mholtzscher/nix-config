---
description: Create or update agent skills using the build-skill skill
---

Use the `build-skill` skill for this request.

If the skill is not already loaded, read its `SKILL.md` first, then follow it to create or update the requested skill.

User request:
$ARGUMENTS
