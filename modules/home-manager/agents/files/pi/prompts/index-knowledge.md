---
description: Generate hierarchical AGENTS.md knowledge base
---

Use the `index-knowledge` skill for this request.

If the skill is not already loaded, read its `SKILL.md` first, then follow it to analyze the current codebase and generate hierarchical `AGENTS.md` files.

User request:
$ARGUMENTS
