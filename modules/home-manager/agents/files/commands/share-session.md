---
description: Upload the current opencode session to the configured receiver
---

Upload the current session using the session-uploader plugin.

If you see this prompt, the plugin did not intercept `/share-session`. Tell the user to check that `session-uploader.ts` is installed and opencode was restarted.
