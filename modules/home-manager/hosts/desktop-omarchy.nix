{ pkgs, config, lib, ... }:

{
  # This file can contain omarchy-specific home-manager configurations
  # that override or supplement the base desktop configuration
  
  # Import any omarchy-specific settings here
  # Most omarchy configuration will come from the omarchy-nix module
}